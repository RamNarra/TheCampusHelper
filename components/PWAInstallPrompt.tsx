import React from 'react';

// PWA has been removed. This component is kept as a no-op to avoid breaking
// historical imports.
const PWAInstallPrompt: React.FC = () => null;

export default PWAInstallPrompt;
