import React from 'react';

// PWA has been removed. This component is kept as a no-op to avoid breaking
// historical imports and to ensure TypeScript doesn't resolve PWA virtual modules.
const PWAUpdatePrompt: React.FC = () => null;

export default PWAUpdatePrompt;
