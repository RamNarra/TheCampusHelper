# --- PUBLIC (Client-Side) ---
# Safe to expose in browser bundle.
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# --- OPTIONAL (Client-side file uploads: PDF/PPTX) ---
# Cloudinary unsigned preset (raw uploads)
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_upload_preset

# --- PRIVATE (Server-Side ONLY; Vercel / serverless) ---
# Never prefix secrets with VITE_.
GEMINI_API_KEY=your_gemini_api_key

# Vercel AI Gateway (optional, recommended for reliability)
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_api_key
# Example: openai/gpt-5, anthropic/claude-sonnet-4
AI_GATEWAY_MODEL=openai/gpt-5
# Optional override
# AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1

# Firebase Admin SDK (Required for server-side token verification)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Upstash Redis (Rate limiting)
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# Optional: CORS allowlist for serverless endpoints (comma-separated origins)
# APP_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Email notifications (optional)

